{
  "name": "jobify",
  "private": true,
  "version": "0.1.0",
  "description": "Jobify - prepares complete job-application dossiers; the human decides.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix desktop",
    "dev": "concurrently -k -n server,desktop -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix desktop\"",
    "dev:server": "npm run dev --prefix server",
    "dev:desktop": "npm run dev --prefix desktop",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "concurrently": "^8.2.2",
    "eslint": "^9.13.0",
    "eslint-plugin-jsdoc": "^50.5.0",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^15.11.0"
  }
}
